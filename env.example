# Firebase Configuration
# Get these values from your Firebase Console (Project Settings > General > Your apps)

# Firebase Project Configuration
# Example values from nextjs-template-a762e project:
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# Firebase Functions Configuration (for server-side operations)
# These are used in Firebase Functions, not in the Next.js app
FIREBASE_PROJECT_ID=nextjs-template-a762e

# Optional: Custom domain configuration
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# =============================================================================
# GITHUB SECRETS SETUP
# =============================================================================
# 
# To set up GitHub Secrets for CI/CD deployment:
# 1. Go to your GitHub repository
# 2. Navigate to Settings > Secrets and variables > Actions
# 3. Click "New repository secret" and add each of these secrets:
#
# Required Secrets for GitHub Actions:
# - FIREBASE_SERVICE_ACCOUNT_KEY: Your Firebase service account JSON (get from Firebase Console > Project Settings > Service Accounts)
#
# Optional Secrets:
# - FIREBASE_TOKEN: Your Firebase CLI token (only needed if not using service account)
# - FIREBASE_PROJECT_ID: Your Firebase project ID (if different from default)
#
# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file to .env for local development:
#    cp env.example .env
#
# 2. Fill in all the values above with your actual credentials
#
# 3. For Firebase credentials:
#    - Go to Firebase Console > Project Settings > General
#    - Scroll down to "Your apps" section
#    - Click on your web app or create a new one
#    - Copy the config values
#
# 4. Deploy your functions with environment variables:
#    firebase deploy --only functions
#    # You'll be prompted to set the function environment variables
#
# =============================================================================
